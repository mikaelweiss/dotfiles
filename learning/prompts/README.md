# Prompt Library

Prompts that have proven to work well.

When you discover a prompt pattern that works:
1. Generalize it (replace specifics with patterns)
2. Add to `tested/` with a descriptive name
3. Note when to use it and what makes it work
